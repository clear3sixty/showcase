---
import { Icon } from 'astro-icon/components';
---

<section id="contact-home" class="py-16 md:py-20 hero-gradient text-theme-on-dark"> {/* Section bg to hero-gradient, text to theme-on-dark */}
    <div class="container mx-auto px-4">
        <div class="text-center mb-12">
            <h2 class="text-3xl md:text-4xl font-bold text-theme-on-dark">Ready to Grow Your Business?</h2> {/* Heading text color */}
            <p class="text-lg md:text-xl mt-4 text-theme-on-dark opacity-90 max-w-2xl mx-auto"> {/* Paragraph text color with slight opacity */}
                Let's talk about how Clear3sixty can help you achieve your digital goals.
            </p>
        </div>

        <div class="max-w-3xl mx-auto bg-white p-8 md:p-10 rounded-xl shadow-xl">
            <h3 class="text-2xl font-bold text-theme-on-light mb-8 text-center">Send Us a Message</h3> {/* Form heading color */}
            <form action="https://formspree.io/f/meokedep" method="POST" class="space-y-6">
                <div>
                    <label for="home-name" class="block text-sm font-medium text-theme-muted mb-1">Full Name</label> {/* Label color */}
                    <div class="relative">
                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                            <Icon name="tabler:user" class="w-5 h-5 text-gray-500" /> {/* Icon color */}
                        </div>
                        <input type="text" name="name" id="home-name" required class="block w-full pl-12 pr-3 py-2.5 border border-gray-300 rounded-md shadow-sm focus:ring-theme-primary focus:border-theme-primary sm:text-sm text-theme-on-light placeholder:text-gray-400" placeholder="e.g. John Doe"> {/* Input text, focus, placeholder */}
                    </div>
                </div>

                <div>
                    <label for="home-email" class="block text-sm font-medium text-theme-muted mb-1">Email Address</label>
                     <div class="relative">
                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                            <Icon name="tabler:mail" class="w-5 h-5 text-gray-500" />
                        </div>
                        <input type="email" name="email" id="home-email" required class="block w-full pl-12 pr-3 py-2.5 border border-gray-300 rounded-md shadow-sm focus:ring-theme-primary focus:border-theme-primary sm:text-sm text-theme-on-light placeholder:text-gray-400" placeholder="you@example.com">
                    </div>
                </div>

                <div>
                    <label for="home-phone" class="block text-sm font-medium text-theme-muted mb-1">Phone Number (Optional)</label>
                    <div class="relative">
                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                            <Icon name="tabler:phone" class="w-5 h-5 text-gray-500" />
                        </div>
                        <input type="tel" name="phone" id="home-phone" class="block w-full pl-12 pr-3 py-2.5 border border-gray-300 rounded-md shadow-sm focus:ring-theme-primary focus:border-theme-primary sm:text-sm text-theme-on-light placeholder:text-gray-400" placeholder="+256 7XX XXX XXX">
                    </div>
                </div>

                <div>
                    <label for="home-subject" class="block text-sm font-medium text-theme-muted mb-1">Subject</label>
                    <div class="relative">
                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                            <Icon name="tabler:heading" class="w-5 h-5 text-gray-500" />
                        </div>
                    <input type="text" name="subject" id="home-subject" required class="block w-full pl-12 pr-3 py-2.5 border border-gray-300 rounded-md shadow-sm focus:ring-theme-primary focus:border-theme-primary sm:text-sm text-theme-on-light placeholder:text-gray-400" placeholder="e.g. Web Design Inquiry">
                    </div>
                </div>

                <div>
                    <label for="home-message" class="block text-sm font-medium text-theme-muted mb-1">Your Message</label>
                    <div class="relative">
                         <div class="absolute top-3 left-0 pl-3 flex items-center pointer-events-none">
                            <Icon name="tabler:message" class="w-5 h-5 text-gray-500" />
                        </div>
                        <textarea name="message" id="home-message" rows="5" required class="block w-full pl-12 pr-3 py-2.5 border border-gray-300 rounded-md shadow-sm focus:ring-theme-primary focus:border-theme-primary sm:text-sm text-theme-on-light placeholder:text-gray-400" placeholder="Tell us about your project or question..."></textarea>
                    </div>
                </div>

                <div>
                    <button type="submit" class="btn-theme-primary w-full flex items-center justify-center px-6 py-3 border border-transparent shadow-sm text-base font-medium transition focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-theme-primary"> {/* Button to theme primary */}
                        <Icon name="tabler:send" class="w-5 h-5 mr-2 -ml-1" />
                        Send Message
                    </button>
                </div>
            </form>
            <p class="text-center text-theme-muted text-sm mt-8"> {/* "Or call us" text color */}
                Or call us directly at <a href="tel:+256759311117" class="font-medium text-theme-primary hover:text-theme-secondary transition">+256 759 311 117</a> {/* Link colors */}
            </p>
        </div>
    </div>
</section>